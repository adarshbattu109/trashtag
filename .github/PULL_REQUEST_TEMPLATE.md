## Summary

<!-- Brief description of what this PR does and why -->

## Changes

<!-- List the key changes made -->

-

## Test Plan

- [ ] New/changed behavior has a runnable check
- [ ] Linting passes (`uv tool run ruff check . && uv tool run ruff format --check .`)
- [ ] Tests pass (`uv run pytest -q`)
- [ ] Ran the server (`uv run trashtag`) and exercised the changed endpoint or dashboard view, if the API or UI changed
- [ ] README updated, if the API contract or configuration changed
- [ ] No secrets or log files committed
